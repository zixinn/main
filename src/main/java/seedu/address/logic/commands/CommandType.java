package seedu.address.logic.commands;

/**
 * Represents the different types of commands.
 */
public enum CommandType {
    MODULE, LESSON, TASK, FACILITATOR, CALENDAR, CLEAR, HELP, EXIT, CMD, PROMPTING, UNDO, REDO
}
